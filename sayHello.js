// original codevto debug
// function sayHello(name) {
//   return 'Hello'
// }


// had to add hello with an extra string to create space, plus add the parameter (name) so it can return  
function sayHello(name) {
  return 'Hello,' + " " + name 
}
